module.exports = function(app, express) {
    //1.1. Review about express.static in https://expressjs.com/es/4x/api.html#express.static
    //Define a static directory with name 'public' for serve static content as index.html, the public directory its on root directory.
    //The index.html file must be accessed as http://localhost:3000/public/index.html


    //1.2: Also add header 'x-powered-by': 'devhack.com'
    const options = {

    };
};