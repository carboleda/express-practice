const dataSource = require('../lib/dataSource.json');

module.exports = function(router) {
    //1.1. Defines a new route GET /define/users that responds all user on dataSource
    //Response example: [{id: 1, name: 'Leanne Graham', ...}, ...]



    //1.2. Defines a new route GET /advanced/users/:id that responds the user with id indicated
    //Response example: {id: 1, name: 'Leanne Graham', ...}



    //1.3. Defines a new route PATCH /advanced/users/:id that update the user with id indicated
    //and responds the user updated. The data to update comes on req.body
    //Response example: {id: 1, name: 'Leanne Graham', ...}
    //NOTE: Review Array.findIndex function in https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Array/findIndex

};