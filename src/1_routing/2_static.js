module.exports = function(app, express) {
    //1.1. Revisa sobre express.static en https://expressjs.com/es/4x/api.html#express.static
    //Definir una ruta a un directorio estatico con el nombre 'public' para servir contenido estatico como index.html.
    //El archivo index.html debe poder ser accesado como http://localhost:3000/public/index.html
    //NOTA: El directorio public esta en el directorio root del proyecto.

    //1.2: Tambien adiciona un header 'x-powered-by': 'devhack.com'
    const options = {

    };

};